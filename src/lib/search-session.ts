// The search page keeps the last query and its results for the length of a tab so
// that leaving Discover and returning does not throw the work away. Global navigation
// never destroys work merely because another page was opened.
export const SNAPSHOT_KEY='bosagezme:search';
