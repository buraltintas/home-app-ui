'use client';import {useState} from 'react';import {AuthDialog} from '@/components/AuthDialog';import {LocaleSwitcher} from '@/components/LocaleSwitcher';import {useI18n} from '@/i18n/I18nProvider';export default function Page(){const {t}=useI18n();const [open,setOpen]=useState(false);return <main className="empty-page"><p className="eyebrow">{t('profile')}</p><h1>{t('profileTitle')}</h1><p>Sign in to manage your reviews and private preferences.</p><button className="button primary" onClick={()=>setOpen(true)}>{t('signIn')}</button><LocaleSwitcher/><AuthDialog open={open} onClose={()=>setOpen(false)}/></main>}

