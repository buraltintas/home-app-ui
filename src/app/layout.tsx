import type {Metadata} from 'next';import {cookies} from 'next/headers';import {Source_Sans_3,Source_Serif_4} from 'next/font/google';import {Header} from '@/components/Header';import {I18nProvider} from '@/i18n/I18nProvider';import type {Locale} from '@/lib/types';import './globals.css';
const sans=Source_Sans_3({variable:'--font-sans',subsets:['latin','cyrillic'],display:'swap'});const serif=Source_Serif_4({variable:'--font-serif',subsets:['latin','cyrillic'],display:'swap'});
const siteUrl=process.env.NEXT_PUBLIC_SITE_URL??'https://bosagezme.com';
export const metadata:Metadata={metadataBase:new URL(siteUrl),applicationName:'Boşa Gezme!',title:{default:'Boşa Gezme! — Real stores, real experiences',template:'%s | Boşa Gezme!'},description:'Discover real physical home and living stores through verified community visits.',alternates:{canonical:'/'},openGraph:{type:'website',url:'/',siteName:'Boşa Gezme!',title:'Boşa Gezme! — Real stores, real experiences',description:'Discover real physical home and living stores through verified community visits.',images:[{url:'/brand/bosagezme-logo.png',width:1254,height:1254,alt:'Boşa Gezme! logo'}]},twitter:{card:'summary_large_image',title:'Boşa Gezme! — Real stores, real experiences',description:'Discover real physical home and living stores through verified community visits.',images:['/brand/bosagezme-logo.png']}};
export default async function RootLayout({children}:{children:React.ReactNode}){const value=(await cookies()).get('bosagezme_locale')?.value;const locale=(['tr','en','de','ru'].includes(value??'')?value:'tr') as Locale;return <html lang={locale} className={`${sans.variable} ${serif.variable}`}><body><I18nProvider initialLocale={locale}><Header/>{children}</I18nProvider></body></html>}
