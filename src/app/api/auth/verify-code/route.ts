import {NextRequest,NextResponse} from 'next/server';import type {TokenPair} from '@/lib/types';
export async function POST(request:NextRequest){const response=await fetch(new URL('/api/proxy/auth/email/verify-code',request.url),{method:'POST',headers:{'content-type':'application/json','x-locale':request.headers.get('x-locale')??'tr'},body:await request.text()});const data=await response.json();const outgoing=NextResponse.json(response.ok?{user_id:(data as TokenPair).user_id}:data,{status:response.status});if(response.ok){const pair=data as TokenPair;const secure=process.env.NODE_ENV==='production';outgoing.cookies.set('homeapp_access',pair.access_token,{httpOnly:true,secure,sameSite:'lax',path:'/',expires:new Date(pair.access_expires_at)});outgoing.cookies.set('homeapp_refresh',pair.refresh_token,{httpOnly:true,secure,sameSite:'strict',path:'/api/auth',expires:new Date(pair.refresh_expires_at)})}return outgoing}

